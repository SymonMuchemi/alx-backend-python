#!/usr/bin/env python3
def sum_list(input_list: list[float]) -> float:
    """Returns the sum of all numbers in a list of floats.

    Args:
        input_list (list[float]): List of floats.

    Returns:
        float: Sum.
    """
    return sum(input_list)
